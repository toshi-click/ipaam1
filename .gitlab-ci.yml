cache:
  key: ${CI_PROJECT_ID}
  paths:
  - .gradle/

before_script:
  - chmod +x ./gradlew

stages:
  - build
#  - test

build:
  stage: build
  script:
    - ./gradlew assemble
#    - ./gradlew lint
#    - ./gradlew checkLicenses
#    - ./gradlew jacocoTestReportDevelop
  artifacts:
    paths:
    - app/build/outputs/
#    - app/build/reports/

#unitTests:
#  stage: test
#  script:
#    - ./gradlew test
#  artifacts:
#    paths:
#    - app/build/reports
